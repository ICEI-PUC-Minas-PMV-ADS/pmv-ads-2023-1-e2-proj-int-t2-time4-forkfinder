@model IEnumerable<fork_finder.Models.RestauranteMesa>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.QuantidadeMesa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TamanhoMesa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RestauranteReserva)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.QuantidadeMesa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TamanhoMesa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RestauranteReserva.RestauranteReservaId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RestauranteMesaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RestauranteMesaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RestauranteMesaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
