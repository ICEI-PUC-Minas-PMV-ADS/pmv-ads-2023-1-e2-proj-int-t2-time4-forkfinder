@*@model Restaurante

@{
    ViewData["Title"] = "Restaurantes";
}

<h1>Mesas</h1>

<p>
    <a asp-controller="Mesas" asp-action="Create">Adicionar nova mesa</a>
</p>

<div class="container">
    <div class="row justify-content-center">
        <div class="col">
            <div id="dateCarousel" class="carousel slide mb-4" data-ride="carousel">
                <div class="carousel-inner">
                    @for (int i = 0; i < Model.Agendas.Count; i++)
                    {
                        <div class="carousel-item @(i == 0 ? "active" : "")">
                            <h4 class="text-center">@Model.Agendas[i].Data.ToString("dd/MM/yyyy")</h4>
                        </div>
                    }
                </div>
                <a class="carousel-control-prev" href="#dateCarousel" role="button" data-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="sr-only">Previous</span>
                </a>
                <a class="carousel-control-next" href="#dateCarousel" role="button" data-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="sr-only">Next</span>
                </a>
            </div>
            <div class="table-responsive">
                <table class="table table-bordered">
                    <tbody>
                        @foreach (var mesa in Model.Mesas)
                        {
                            <tr>
                                <th>@mesa.TamanhoMesa</th>
                                @foreach (var agenda in Model.Agendas)
                                {
                                    <td>
                                        @if (agenda.Horarios.Any(h => h.MesaId == mesa.Id))
                                        {
                                            var horario = agenda.Horarios.First(h => h.MesaId == mesa.Id);
                                            <p>
                                                <a href="@Url.Action("Reserva", "Agendamento", new { restauranteId = Model.Id, mesaId = mesa.Id, horarioId = horario.Id, clienteId = "seu_cliente_id" })">
                                                    @horario.Hora
                                                </a>
                                            </p>
                                        }
                                        else
                                        {
                                            <p>Não disponível</p>
                                        }
                                    </td>
                                }
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script>
        $(function () {
            $("#dateCarousel").on("slid.bs.carousel", function () {
                var activeDate = $(this).find(".carousel-item.active h4").text().trim();
                var selectedDate = new Date(activeDate);
            });
        });
    </script>
}
*@
@model Restaurante

@{
    ViewData["Title"] = "Restaurantes";
}

<h1>Mesas</h1>

<p>
    <a asp-controller="Mesas" asp-action="Create">Adicionar nova mesa</a>
</p>

<div class="container">
    <div class="row justify-content-center">
        <div class="col">
            <div id="dateCarousel" class="carousel slide mb-4">
                <div class="carousel-inner">
                    @for (int i = 0; i < Model.Agendas.Count; i++)
                    {
                        <div class="carousel-item @(i == 0 ? "active" : "")">
                            <h4 class="text-center">@Model.Agendas[i].Data.ToString("dd/MM/yyyy")</h4>
                            <div class="table-responsive">
                                <table class="table table-bordered">
                                    <thead>
                                        <tr>
                                            <th>Mesa</th>
                                            @*@foreach (var horario in Model.Agendas[i].Horarios.OrderBy(h => h.Hora))
                                            {
                                                <th>@horario.Hora</th>
                                            }*@
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var mesa in Model.Mesas)
                                        {
                                            <tr>
                                                <td>@mesa.TamanhoMesa</td>
                                                @foreach (var horario in Model.Agendas[i].Horarios.OrderBy(h => h.Hora))
                                                {
                                                    <td>
                                                        @horario.Hora
                                                        @*@if (mesa.Horarios.Any(h => h.Id == horario.Id))
                                                        {
                                                            @horario.Hora
                                                        }
                                                        else
                                                        {
                                                            <p>Não disponível</p>
                                                        }*@
                                                    </td>
                                                    <td>
                                                        Teste
                                                    </td>
                                                }
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    }
                </div>
                <a class="carousel-control-prev" href="#dateCarousel" role="button" data-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="sr-only">Previous</span>
                </a>
                <a class="carousel-control-next" href="#dateCarousel" role="button" data-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="sr-only">Next</span>
                </a>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js"></script>
    <script>
        $(function () {
            $("#dateCarousel").carousel({
                interval: false
            });

            $("#dateCarousel").on("slid.bs.carousel", function () {
                var activeItem = $(this).find(".carousel-item.active");
                var activeDate = activeItem.find("h4").text().trim();
                var selectedDate = new Date(activeDate);
            });
        });
    </script>
}
